export const ROUTES = {
	main: '/',
	auth: {
		login: '/login',
		register: '/register',
	},
	spreads: {
		threeCard: '/three-card',
	},
};

export const generateUrl = (url: string) => {
	return url;
};
